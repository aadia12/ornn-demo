# Python
__pycache__/
*.pyc
.venv/
venv/

# Generated outputs (regenerated by running the scripts)
hedge_results.png

# Raw API snapshots: useful locally as an audit trail, too bulky for git
data/raw/

# Keep data/ocpi_history.csv tracked — that's the accumulating price history

# Secrets — never commit an API key
.env

# Editor / OS noise
.vscode/
.DS_Store
Thumbs.db
